pipeline {
    agent any

    stages {
		stage('Build') {
			steps {
				echo 'Build app'
			}
		}
	stage('Test') {
		    steps {
				echo 'Test app'
				echo 'test trigger from github'
				sh 'ssh root@10.184.0.13 whoami'
			}
		}
	stage('Deploy') {
			steps {
				echo 'Deploy app'
			}
		}
									
	}
}
